import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    marginBottom: 16
  },

  label: {
    marginBottom: 6,
    color: "#333"
  },

  input: {
    height: 44,
    borderWidth: 1,
    borderColor: "#DDD",
    borderRadius: 8,
    paddingHorizontal: 12,
    fontSize: 16
  }
})
